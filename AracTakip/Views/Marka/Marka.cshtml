@model List<AracTakipSistemi.DAL.Concrete.tbl_Marka>
@{
    ViewBag.Title = "Marka";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Marka</h2>
<table class="table table-bordered">
    <thead>
        <tr>
            <th scope="col">Marka AD</th>
            <th scope="col">Sil</th>
            <th scope="col">Güncelle</th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td scope="row">@item.MarkaAD</td>
                <td><a href="/Marka/DeleteMarka/@item._id" class="btn btn-danger">Sil</a></td>
                <td><a href="/Marka/GetMarka/@item._id" class="btn btn-warning">Güncelle</a></td>
            </tr>
        }

    </tbody>
</table>
